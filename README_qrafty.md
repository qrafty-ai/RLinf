# Installation (pixi+xvla)
```bash
pixi install
uv venv -p .pixi/envs/default/bin/python --seed
direnv allow # or sourrce directly
bash requirements/install.sh embodied --model xvla --env maniskill_libero --install-rlinf
```
